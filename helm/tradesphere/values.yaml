replicaCount: 2

image:
  repository: tradesphere
  tag: latest
  pullPolicy: Never       # use local image in kind

service:
  port: 80
  targetPort: 5000

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

secretKey: "my-production-secret-key"