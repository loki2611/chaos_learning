name: Chaos Learning Pipeline
on:
  workflow_dispatch:
  push:
      branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test Pipeline
        run: echo "GitHub Actions is connected!"